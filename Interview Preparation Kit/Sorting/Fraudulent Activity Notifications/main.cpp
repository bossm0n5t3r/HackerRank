#include <iostream>

using namespace std;

int expenditure[200001];

int main() {
  // 1.) Counting sort
  // 2.) A Queue
  // 3.) Pay attention to the even case.
  // 4.) Integer division is a blessing and a curse, be careful.
  ios::sync_with_stdio(0);
  cin.tie(0);
  freopen("input05.txt", "r", stdin);
  int n, d;
  cin >> n >> d;
  return 0;
}
